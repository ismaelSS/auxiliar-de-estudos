---
phase: quick-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/IsmaelSS/model/Question.java
  - src/main/java/org/IsmaelSS/model/RoundResult.java
  - src/main/java/org/IsmaelSS/model/RoundState.java
  - src/main/java/org/IsmaelSS/service/ThemeLoader.java
  - src/main/java/org/IsmaelSS/service/StatsService.java
  - themes/java-record.json
  - themes/java-modificadores.json
  - src/test/java/org/IsmaelSS/model/RoundStateReinforcementTest.java
  - src/test/java/org/IsmaelSS/service/StatsServiceTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "Question.id is an int instead of String"
    - "JSON theme files use integer IDs (0, 1, 2, ...) instead of string IDs"
    - "RoundResult.questionId is an int"
    - "Stats file still serializes correctly (question IDs stored as strings in JSON)"
    - "All existing tests pass after migration"
    - "Reinforcement mode still correctly identifies weak questions"
    - "ThemeLoader still validates questions correctly (no null/empty id check for int)"
  artifacts:
    - path: "src/main/java/org/IsmaelSS/model/Question.java"
      provides: "Model with int id field"
      min_lines: 33
    - path: "src/main/java/org/IsmaelSS/model/RoundResult.java"
      provides: "Record with int questionId"
      min_lines: 1
    - path: "themes/java-record.json"
      provides: "Question bank with integer IDs"
      contains: "\"id\": 0"
    - path: "themes/java-modificadores.json"
      provides: "Question bank with integer IDs"
      contains: "\"id\": 0"
  key_links:
    - from: "Question.java"
      to: "RoundResult.java"
      via: "questionId type int"
      pattern: "int questionId"
    - from: "RoundResult.java"
      to: "StatsService.java"
      via: "recordRound reads questionId()"
    - from: "Question.java"
      to: "RoundState.java"
      via: "q.getId() in reinforcement matching"
    - from: "themes/*.json"
      to: "ThemeLoader.java"
      via: "Jackson deserializes int id"
---

<objective>
Change Question.id from String to int (incremental number) across all model classes, JSON theme files, and consumers.

**Purpose:** The question IDs in JSON theme files were arbitrary strings, but should be sequential integers for simplicity and consistency.

**Output:**
- `Question.java` — `id` field changed from `String` to `int`
- `RoundResult.java` — `questionId` changed from `String` to `int`
- `RoundState.java` — uses `String.valueOf(q.getId())` to match against stats String-keyed sets/maps
- `ThemeLoader.java` — validation updated (check `id < 0` instead of null/empty)
- `StatsService.java` — `result.questionId()` converted to String for stats map key
- `themes/java-record.json` — string IDs replaced with integers 0-34
- `themes/java-modificadores.json` — string IDs replaced with integers 0-29
- Tests updated to use int IDs
</objective>

<execution_context>
</execution_context>

<context>
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/model/Question.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/model/RoundResult.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/model/RoundState.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/service/ThemeLoader.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/service/StatsService.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/controller/StudyRoundController.java
@C:/prog/flashCardJava/src/main/java/org/IsmaelSS/model/StatsData.java
@C:/prog/flashCardJava/themes/java-record.json
@C:/prog/flashCardJava/themes/java-modificadores.json
@C:/prog/flashCardJava/src/test/java/org/IsmaelSS/model/RoundStateReinforcementTest.java
@C:/prog/flashCardJava/src/test/java/org/IsmaelSS/service/StatsServiceTest.java
@C:/prog/flashCardJava/src/test/java/org/IsmaelSS/model/StatsDataTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change Question.id to int and update all consumers</name>
  <files>
    src/main/java/org/IsmaelSS/model/Question.java,
    src/main/java/org/IsmaelSS/model/RoundResult.java,
    src/main/java/org/IsmaelSS/model/RoundState.java,
    src/main/java/org/IsmaelSS/service/ThemeLoader.java,
    src/main/java/org/IsmaelSS/service/StatsService.java
  </files>
  <action>
    **1. `Question.java`** — Change `id` field from `String` to `int`:
    - Replace `private String id;` → `private int id;`
    - Replace constructor param `@JsonProperty("id") String id` → `@JsonProperty("id") int id`
    - Replace `this.id = id;` (stays but now assigns int)
    - Replace `getId()` return type `String` → `int`
    - Replace `setId(String id)` → `setId(int id)` param type

    **2. `RoundResult.java`** — Change `questionId` from `String` to `int`:
    - Replace `record RoundResult(String themeName, String questionText, String questionId, boolean wasCorrect)`
      → `record RoundResult(String themeName, String questionText, int questionId, boolean wasCorrect)`

    **3. `RoundState.java`** — Update reinforcement code where `q.getId()` (now int) is used against String-keyed sets/maps:
    - Line 69: `if (errorIds.contains(q.getId()))` → `if (errorIds.contains(String.valueOf(q.getId())))`
    - Lines 77-78: `scoreMap.getOrDefault(a.getId(), 0)` and `scoreMap.getOrDefault(b.getId(), 0)`
      → `scoreMap.getOrDefault(String.valueOf(a.getId()), 0)` and same for b
    No other changes needed in this file.

    **4. `ThemeLoader.java`** — Update validation for int IDs:
    - Line 67: `Set<String> seenIds = new HashSet<>()` → `Set<Integer> seenIds = new HashSet<>()`
    - Lines 78-84: Replace the null/empty check and duplicate check block:
      ```java
      if (q.getId() == null || q.getId().isEmpty()) {
          LOG.warning("Question at index " + i + " in file: " + file.getName() + " has no id");
          return null;
      }
      if (!seenIds.add(q.getId())) {
          LOG.warning("Duplicate question id '" + q.getId() + "' at index " + i + " in file: " + file.getName());
          return null;
      }
      ```
      Replace with:
      ```java
      if (q.getId() < 0) {
          LOG.warning("Question at index " + i + " in file: " + file.getName() + " has invalid id: " + q.getId());
          return null;
      }
      if (!seenIds.add(q.getId())) {
          LOG.warning("Duplicate question id " + q.getId() + " at index " + i + " in file: " + file.getName());
          return null;
      }
      ```
      Note: `q.getId()` returns `int`, so both `q.getId() < 0` and `seenIds.add(q.getId())` work via autoboxing.

    **5. `StatsService.java`** — Line 99: Convert int questionId to String for map key:
    - `result.questionId()` is now `int` instead of `String`
    - Change `.computeIfAbsent(result.questionId(), k -> new QuestionScore())`
      → `.computeIfAbsent(String.valueOf(result.questionId()), k -> new QuestionScore())`

    **6. `StudyRoundController.java`** — No changes needed. `roundState.getCurrentQuestion().getId()` returns `int` and is passed to `new RoundResult(..., ..., ..., correct)` where the 3rd parameter is now `int`. Auto-works.

    **Verify:** `mvn compile` succeeds with no errors.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; mvn compile -q 2>&1</automated>
  </verify>
  <done>mvn compile passes, Question.id is int, RoundResult.questionId is int, all consumers compile.</done>
</task>

<task type="auto">
  <name>Task 2: Update JSON theme files — replace string IDs with sequential integers</name>
  <files>
    themes/java-record.json,
    themes/java-modificadores.json
  </files>
  <action>
    **`themes/java-record.json`** — Replace each string ID with sequential integers 0 through 34:
    - `"id": "record-primary-purpose"` → `"id": 0`
    - `"id": "record-component-visibility"` → `"id": 1`
    - `"id": "record-accessor-method"` → `"id": 2`
    - Follow existing file order, assign increasing integers (0, 1, 2, ..., 34)
    - All other fields (question, options, correct) remain unchanged

    **`themes/java-modificadores.json`** — Replace each string ID with sequential integers 0 through 29:
    - `"id": "mod-public-access"` → `"id": 0`
    - `"id": "mod-private-access"` → `"id": 1`
    - `"id": "mod-package-private"` → `"id": 2`
    - Follow existing file order, assign increasing integers (0, 1, 2, ..., 29)
    - All other fields remain unchanged

    Use the exact same order as the existing JSON arrays (verified by grep output that shows 35 record IDs and 30 modificadores IDs).
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; mvn compile -q 2>&1</automated>
  </verify>
  <done>JSON files use integer IDs; mvn compile still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests to use int IDs</name>
  <files>
    src/test/java/org/IsmaelSS/model/RoundStateReinforcementTest.java,
    src/test/java/org/IsmaelSS/service/StatsServiceTest.java
  </files>
  <action>
    **`RoundStateReinforcementTest.java`:**

    Change `createQ` helper signature from `String id` to `int id`:
    ```java
    private Question createQ(int id, String text, String... options) {
        return new Question(id, text, Arrays.asList(options), 0);
    }
    ```

    Change `collectIds` return type from `List<String>` to `List<Integer>`:
    ```java
    private List<Integer> collectIds(RoundState state) {
        List<Integer> ids = new ArrayList<>();
        while (!state.isComplete()) {
            ids.add(state.getCurrentQuestion().getId());
            state.advanceToNext();
        }
        return ids;
    }
    ```

    Update all test data to use int IDs (sequential per test):

    **Test `createReinforcementRoundWithEmptyStatsReturnsCorrectCount`:**
    - `createQ("t1q1", ...)` → `createQ(0, ...)`
    - `createQ("t1q2", ...)` → `createQ(1, ...)`
    - ...through `createQ(4, ...)`

    **Test `createReinforcementRoundPrioritizesLowestScoreQuestions`:**
    - RoundResults: `new RoundResult("t1", "text1", 0, false)` (was `"t1q1"`), etc.
    - `createQ("t1q1", ...)` → `createQ(0, ...)`, etc.
    - `ids.contains("t1q1")` → `ids.contains(0)`; `ids.contains("t1q2")` → `ids.contains(1)`

    **Test `createReinforcementRoundFillsRemainingWithFreshWhenFewErrors`:**
    - RoundResults: `new RoundResult("t1", "text1", 0, false)` (was `"t1q1"`)
    - `createQ("t1q1", ...)` → `createQ(0, ...)`, etc.
    - `ids.contains("t1q1")` → `ids.contains(0)`
    - `id -> !id.equals("t1q1")` → `id -> id != 0`

    **Test `createReinforcementRoundRespectsQuestionCountPerTheme`:**
    - `createQ("q1", ...)` → `createQ(0, ...)` through `createQ(9, ...)`

    **Test `normalConstructorWorksBackwardCompatible`:**
    - `createQ("id_a", ...)` → `createQ(0, ...)` through `createQ(4, ...)`

    **Test `normalConstructorDoesNotUseStatsService`:**
    - `createQ("only_q", ...)` → `createQ(0, ...)`
    - `assertEquals("only_q", ...)` → `assertEquals(0, ...)`

    **Test `createReinforcementRoundMultipleThemesAndCrossThemeLowScores`:**
    - RoundResults: use int IDs (m1=0, m2=1, h1=3 — unique per question)
    - `createQ("m1", ...)` → `createQ(0, ...)` through `createQ(2, ...)`
    - `createQ("h1", ...)` → `createQ(3, ...)` through `createQ(5, ...)`
    - `ids.contains("m1")` → `ids.contains(0)`
    - `ids.contains("m2")` → `ids.contains(1)`
    - `ids.contains("h1")` → `ids.contains(3)`

    **`StatsServiceTest.java`:**

    Update all `new RoundResult(...)` calls — change the 3rd argument from String questionId to int questionId:
    - `new RoundResult("t1", "q1 text?", "q1", true)` → `new RoundResult("t1", "q1 text?", 0, true)`
    - `new RoundResult("t1", "q2?", "q2", false)` → `new RoundResult("t1", "q2?", 1, false)`
    - `new RoundResult("t1", "q3?", "q3", false)` → `new RoundResult("t1", "q3?", 2, false)`
    - `new RoundResult("t2", "q2?", "q2", false)` → `new RoundResult("t2", "q2?", 0, false)` (different theme, so ID 0 is fine)
    - In `scoreCapsAtFive`: `new RoundResult("t1", "q1 text?", "q1", true)` → `new RoundResult("t1", "q1 text?", 0, true)`
    - In `scoreFloorsAtMinusTen`: same
    - In `persistDataAcrossInstances`: `new RoundResult("t1", "q1?", "q1", true)` → `new RoundResult("t1", "q1?", 0, true)`

    Update assertions that compare question IDs (StatsServiceTest lines 119-124):
    - `assertEquals("q3", lowest.get(0).getKey())` → `assertEquals("2", lowest.get(0).getKey())`
    - `assertEquals("q2", lowest.get(1).getKey())` → `assertEquals("1", lowest.get(1).getKey())`
    - `assertEquals("q1", lowest.get(2).getKey())` → `assertEquals("0", lowest.get(2).getKey())`
    Reason: StatsData.ThemeStats.questions remains `Map<String, QuestionScore>`, so question IDs are stored as strings `"0"`, `"1"`, `"2"` (via `String.valueOf(int)`).
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; mvn test -q 2>&1</automated>
  </verify>
  <done>All ~26+ existing tests pass with int IDs.</done>
</task>

</tasks>

<verification>
1. `mvn compile` passes with no errors
2. `mvn test` passes — all existing tests green
3. Question.id is consistently `int` across all Java files
4. JSON theme files use `"id": <number>` format
</verification>

<success_criteria>
- `mvn compile` succeeds
- `mvn test` passes (all tests green)
- Question IDs in JSON files are integers (0, 1, 2, ...)
- No remaining references to String question IDs in Java source code
</success_criteria>

<output>
Created by: gsd-plan-phase (quick mode)
Commit plan file and changes when done.
</output>
