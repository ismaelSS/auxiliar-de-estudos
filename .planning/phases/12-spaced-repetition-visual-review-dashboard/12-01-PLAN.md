---
phase: 12-spaced-repetition-visual-review-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/IsmaelSS/model/FixationPhase.java
  - src/main/java/org/IsmaelSS/model/StatsData.java
  - src/main/java/org/IsmaelSS/service/StatsService.java
  - src/main/java/org/IsmaelSS/model/RoundState.java
  - src/test/java/org/IsmaelSS/model/QuestionScoreSM2Test.java
  - src/test/java/org/IsmaelSS/service/StatsServiceSM2Test.java
  - src/test/java/org/IsmaelSS/model/RoundStateDueReviewTest.java
autonomous: true
requirements: [V2-01, SR-01, SR-02, SR-06, SR-07, SR-08]

must_haves:
  truths:
    - "SM-2 algorithm correctly advances interval and easeFactor on consecutive correct answers"
    - "SM-2 resets interval to 1 and drops repCount on wrong answer"
    - "easeFactor clamps between 1.3 and 2.5"
    - "FixationPhase is correctly derived from repCount and interval thresholds"
    - "recordRound() applies SM-2 update only (old score field no longer updated per D-02)"
    - "getDueQuestions() returns questions where nextReviewTimestamp <= now"
    - "getTimelineData() aggregates lastReviewTimestamp by day per theme"
    - "markThemeAsDone() advances SM-2 for overdue questions only"
    - "createDueReviewRound() picks overdue questions first, falls back to new/unreviewed"
    - "Old JSON files without SM-2 fields deserialize correctly to defaults"
  artifacts:
    - path: "src/main/java/org/IsmaelSS/model/FixationPhase.java"
      provides: "Enum for Aprendendo/Revisão/Fixa/Domínio fixation phases"
      contains: "enum FixationPhase"
    - path: "src/main/java/org/IsmaelSS/model/StatsData.java"
      provides: "QuestionScore with SM-2 fields (easeFactor, interval, repCount, timestamps)"
      contains: "updateSM2"
    - path: "src/main/java/org/IsmaelSS/service/StatsService.java"
      provides: "SM-2 recordRound integration, due/timeline queries, markThemeAsDone"
      contains: "getDueQuestions"
    - path: "src/main/java/org/IsmaelSS/model/RoundState.java"
      provides: "createDueReviewRound factory for due-only review rounds"
      contains: "createDueReviewRound"
  key_links:
    - from: "src/main/java/org/IsmaelSS/model/StatsData.java"
      to: "src/main/java/org/IsmaelSS/model/FixationPhase.java"
      via: "QuestionScore.getFixationPhase() returns FixationPhase enum"
      pattern: "getFixationPhase\\(\\)"
    - from: "src/main/java/org/IsmaelSS/service/StatsService.java"
      to: "src/main/java/org/IsmaelSS/model/StatsData.java"
      via: "recordRound calls updateSM2 on QuestionScore"
      pattern: "updateSM2"
    - from: "src/main/java/org/IsmaelSS/model/RoundState.java"
      to: "src/main/java/org/IsmaelSS/service/StatsService.java"
      via: "createDueReviewRound calls getDueQuestions/getNewQuestions"
      pattern: "getDueQuestions"
---

Artifacts this phase produces:
- `src/main/java/org/IsmaelSS/model/FixationPhase.java` — NEW enum
- SM-2 fields + methods on `QuestionScore` in StatsData.java — MODIFIED
- 7 new query/update methods on StatsService.java — MODIFIED
- `RoundState.createDueReviewRound()` factory method — MODIFIED
- 3 new test files covering SM-2 logic, service queries, and round factory

---

<objective>
Build the complete SM-2 spaced repetition data layer: model fields, algorithm methods, service queries, round factory, and unit tests.

Purpose: Every downstream component (dashboard views, controller wiring, timeline) depends on this layer being correct and tested. This plan produces the full data foundation with zero UI code.

Output: FixationPhase enum, QuestionScore with SM-2 updateSM2()/getFixationPhase(), StatsService with 7 new methods, RoundState with createDueReviewRound(), and 3 test classes passing.
</objective>

<execution_context>
@C:/Users/ismae/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/ismae/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-spaced-repetition-visual-review-dashboard/12-CONTEXT.md
@.planning/phases/12-spaced-repetition-visual-review-dashboard/12-RESEARCH.md
@.planning/phases/12-spaced-repetition-visual-review-dashboard/12-PATTERNS.md
@src/main/java/org/IsmaelSS/model/StatsData.java
@src/main/java/org/IsmaelSS/service/StatsService.java
@src/main/java/org/IsmaelSS/model/RoundState.java
@src/test/java/org/IsmaelSS/model/StatsDataTest.java
@src/test/java/org/IsmaelSS/service/StatsServiceTest.java
@src/test/java/org/IsmaelSS/model/RoundStateReinforcementTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: FixationPhase enum + QuestionScore SM-2 fields and methods</name>
  <files>
    src/main/java/org/IsmaelSS/model/FixationPhase.java,
    src/main/java/org/IsmaelSS/model/StatsData.java
  </files>
  <action>
Create the FixationPhase enum in a new file:

`src/main/java/org/IsmaelSS/model/FixationPhase.java` — an enum with 4 values: APRENDENDO, REVISAO, FIXA, DOMINIO. No logic, just the enum constants. Package: org.IsmaelSS.model.

Then modify `src/main/java/org/IsmaelSS/model/StatsData.java`:

1. Add 5 new fields to the existing `QuestionScore` inner class (per D-02, D-03):
   - `private double easeFactor = 2.5;` (default 2.5 — per locked decision)
   - `private int interval;` (days, default 0)
   - `private int repCount;` (consecutive correct, default 0)
   - `private long lastReviewTimestamp;` (millis since epoch, default 0)
   - `private long nextReviewTimestamp;` (millis since epoch, default 0)

2. Keep ALL existing code unchanged: `score` field, `recordCorrect()`, `recordWrong()`, the `@JsonIgnoreProperties(ignoreUnknown = true)` annotation. The old score field and methods must remain for backward compatibility.

3. Add getters and setters for all 5 new fields (standard Java bean pattern matching existing `getScore()`/`setScore()` style).

4. Add `updateSM2(boolean correct)` method — per D-02 locked decision (binary correct/wrong, correct->4 equivalent, wrong->1 equivalent):
   - If correct: `easeFactor = Math.min(2.5, easeFactor + 0.1)`. If `repCount == 0 || interval <= 1`, set `interval = 1`. Else `interval = (int) Math.ceil(interval * easeFactor)`. Increment `repCount`. **CRITICAL PITFALL from RESEARCH.md: First review always sets interval = 1, not ceil(1 * EF).**
   - If wrong: `easeFactor = Math.max(1.3, easeFactor - 0.2)`. Set `interval = 1`. Set `repCount = 0`.
   - Always: Set `lastReviewTimestamp = System.currentTimeMillis()`. Set `nextReviewTimestamp = System.currentTimeMillis() + (long) interval * 86_400_000L`.

5. Add `getFixationPhase()` method returning `FixationPhase` — per D-03 fixation phase thresholds:
   - `repCount == 0 || interval <= 1` → APRENDENDO
   - `repCount <= 2 && interval <= 7` → REVISAO
   - `repCount <= 5 && interval <= 30` → FIXA
   - Otherwise → DOMINIO

Add import for `java.lang.Math` (implicit) and the new enum.

Do NOT add a `@JsonCreator` or change the no-arg constructor — Jackson field defaults handle backward compat automatically (easeFactor=2.5 via field initializer, everything else defaults to 0).
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava && mvn compile -q 2>&1 | findstr /i "error" || echo "COMPILE OK"</automated>
  </verify>
  <done>
    - FixationPhase.java exists with 4 enum constants
    - QuestionScore has 5 new SM-2 fields with getters/setters
    - updateSM2(boolean) correctly implements SM-2 binary variant per locked decisions
    - getFixationPhase() returns correct enum per threshold table
    - Existing score/recordCorrect/recordWrong methods unchanged
    - @JsonIgnoreProperties annotation unchanged
    - `mvn compile` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: StatsService SM-2 integration + query methods + RoundState factory</name>
  <files>
    src/main/java/org/IsmaelSS/service/StatsService.java,
    src/main/java/org/IsmaelSS/model/RoundState.java
  </files>
  <action>
**StatsService.java modifications:**

1. Update `recordRound()` (lines 89-108) — per D-02, SM-2 replaces old score logic:
    - After the existing `qScore.computeIfAbsent(...)` line, replace the if/else block (lines 100-104) with:
      - `qScore.updateSM2(result.wasCorrect())` — SM-2 is the primary update
    - The old `recordCorrect()`/`recordWrong()` methods on QuestionScore remain defined (for any external references) but are no longer called from `recordRound()`.

2. Add new imports: `java.time.Instant`, `java.time.LocalDate`, `java.time.ZoneId`, `java.util.Comparator`, `java.util.EnumMap`, `java.util.TreeMap`, `java.util.Collections`, `org.IsmaelSS.model.FixationPhase`.

3. Add these 6 new methods to StatsService (follow the existing query method pattern — e.g., getDominio at lines 161-169):

   a. `public List<Map.Entry<String, QuestionScore>> getDueQuestions(String themeName)` — stream filter on `nextReviewTimestamp <= System.currentTimeMillis()` AND `nextReviewTimestamp > 0`. Sort by repCount ascending.

   b. `public int getDueCount(String themeName)` — delegates to `getDueQuestions(themeName).size()`.

   c. `public List<Map.Entry<String, QuestionScore>> getNewQuestions(String themeName)` — stream filter on `repCount == 0`.

   d. `public Map<FixationPhase, Integer> getFixationPhases(String themeName)` — EnumMap aggregation over all QuestionScore in theme, calling `qs.getFixationPhase()`. Initialize all counts to 0.

   e. `public Map<LocalDate, Map<String, Integer>> getTimelineData()` — TreeMap (reverse order) keyed by LocalDate. For every QuestionScore with `lastReviewTimestamp > 0`, convert to LocalDate and merge theme name → count. Use `Instant.ofEpochMilli().atZone(ZoneId.systemDefault()).toLocalDate()`.

   f. `public void markThemeAsDone(String themeName)` — per D-05: iterate overdue questions (nextReviewTimestamp <= now AND > 0), call `qs.updateSM2(true)` on each, then `save()`. Only overdue questions are affected.

**RoundState.java modification:**

Add a new static factory method `createDueReviewRound(Theme theme, StatsService statsService)` — per D-06:

1. Call `statsService.getDueQuestions(theme.getName())` to get overdue questions.
2. Build a `Map<Integer, Question> questionMap` from `theme.getQuestions()` keyed by `q.getId()`.
3. Map due entries to Question objects: parse key as int, look up in questionMap, add non-null to `dueQuestions` list.
4. If `dueQuestions` is empty, fall back to `statsService.getNewQuestions(theme.getName())` and map those instead.
5. Shuffle `dueQuestions`. Build `RoundQuestion` list using the exact same shuffle pattern as existing `buildQuestions()` (lines 41-46): get original options, get correct text, shuffle copy, find new index.
6. Return `new RoundState(questions, true)`.

Add imports: `org.IsmaelSS.service.StatsService`, `java.util.HashMap`, `java.util.Map`. The `Theme` and `Question` types are already imported (used by existing methods).
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava && mvn compile -q 2>&1 | findstr /i "error" || echo "COMPILE OK"</automated>
  </verify>
  <done>
    - recordRound() calls updateSM2() on QuestionScore only (recordCorrect/recordWrong no longer called)
    - Old score methods remain defined but unused by recordRound
    - getDueQuestions returns questions with nextReviewTimestamp <= now
    - getDueCount returns correct count
    - getNewQuestions returns questions with repCount == 0
    - getFixationPhases returns EnumMap with counts per phase
    - getTimelineData returns TreeMap<LocalDate, Map<String, Integer>> in reverse order
    - markThemeAsDone only affects overdue questions
    - createDueReviewRound picks overdue first, falls back to new, shuffles, builds RoundQuestion list
    - `mvn compile` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for SM-2 model, service queries, and round factory</name>
  <files>
    src/test/java/org/IsmaelSS/model/QuestionScoreSM2Test.java,
    src/test/java/org/IsmaelSS/service/StatsServiceSM2Test.java,
    src/test/java/org/IsmaelSS/model/RoundStateDueReviewTest.java
  </files>
  <action>
Create 3 new test files. Follow the existing JUnit 5 pattern from StatsDataTest.java: package declaration, imports, `@Test` methods with Arrange → Act → Assert.

**File 1: `src/test/java/org/IsmaelSS/model/QuestionScoreSM2Test.java`**

Tests for QuestionScore SM-2 logic:
- `intervalIncreasesOnCorrect()`: Create QuestionScore (defaults: ef=2.5, interval=0, rep=0). Call updateSM2(true). Assert interval=1, repCount=1, easeFactor=2.6. Call updateSM2(true) again. Assert interval=3 (ceil(1*2.6)=3 per RESEARCH.md), repCount=2.
- `intervalResetsOnWrong()`: Do 3 correct updates, then one wrong. Assert interval=1, repCount=0, easeFactor clamped.
- `easeFactorClamps()`: Start at 2.5, call updateSM2(true) 10 times. Assert never exceeds 2.5. Start at 1.3, call updateSM2(false) 5 times. Assert never below 1.3.
- `fixationPhases()`: Test all 4 phase boundaries:
  - repCount=0 → APRENDENDO
  - repCount=1, interval=1 → APRENDENDO (interval<=1 overrides)
  - repCount=1, interval=3 → REVISAO
  - repCount=2, interval=5 → REVISAO
  - repCount=3, interval=8 → FIXA
  - repCount=5, interval=25 → FIXA
  - repCount=6, interval=31 → DOMINIO
- `timestampsSet()`: After updateSM2, assert lastReviewTimestamp > 0 and nextReviewTimestamp > lastReviewTimestamp.

**File 2: `src/test/java/org/IsmaelSS/service/StatsServiceSM2Test.java`**

Tests for StatsService SM-2 integration:
- `recordRoundAppliesSM2()`: Create StatsService (constructor creates fresh StatsData). Record a round with 2 correct results and 1 wrong. Check that QuestionScore objects have repCount > 0 for correct ones, repCount = 0 for wrong one. Check nextReviewTimestamp > 0.
- `getDueQuestions()`: Record a round, then manually set a question's nextReviewTimestamp to past (System.currentTimeMillis() - 86_400_000L). Call getDueQuestions. Assert it returns that question.
- `getNewQuestions()`: Get questions for a theme with no prior data. Assert returns empty (no questions tracked yet). Record 1 round, then getNewQuestions should return questions with repCount=0... wait, after recording they'd have repCount incremented. Better: check that themes without any data return empty list.
- `getFixationPhases()`: Record several rounds. Call getFixationPhases. Assert result is a non-empty EnumMap.
- `getTimelineData()`: Record a round. Call getTimelineData. Assert result contains today's date as a key.
- `markThemeAsDone()`: Record a round with wrong answer. Get due count (should be 0 since just reviewed). Manually set nextReviewTimestamp to past. Call markThemeAsDone. Get due count (should be 0 now — all marked done). Check that repCount increased.

**File 3: `src/test/java/org/IsmaelSS/model/RoundStateDueReviewTest.java`**

Tests for createDueReviewRound:
- `picksDueQuestions()`: Create a Theme with 3 questions. Create a StatsService, record a round. Manually modify nextReviewTimestamp to past for 2 of them. Call createDueReviewRound. Assert the returned RoundState has exactly 2 questions (only the due ones).
- `fallsBackToNewQuestions()`: Create a Theme with 3 questions. Create a StatsService with no data. Call createDueReviewRound. Assert RoundState has 3 questions (all are new/unreviewed, repCount=0 → getNewQuestions returns them).

Note: For tests that need to manipulate nextReviewTimestamp directly, access the QuestionScore via statsService.getThemeStats(themeName).getQuestions().get(String.valueOf(questionId)). This works because the test creates the StatsService in-memory and can access the data.

For test simplicity, you can construct StatsService, record a round to populate data, then modify the QuestionScore fields directly for setup. Use `java.lang.reflect` or add a package-private setter if needed — but prefer the existing public setters (setNextReviewTimestamp, setRepCount, etc.) that were added in Task 1.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava && mvn test -Dtest="QuestionScoreSM2Test,StatsServiceSM2Test,RoundStateDueReviewTest" -q 2>&1</automated>
  </verify>
  <done>
    - QuestionScoreSM2Test: all 5 tests pass (interval, wrong reset, clamp, fixation phases, timestamps)
    - StatsServiceSM2Test: all 6 tests pass (recordRound SM2, due queries, new queries, fixation, timeline, mark done)
    - RoundStateDueReviewTest: all 2 tests pass (due questions, fallback to new)
    - All new tests use JUnit 5 @Test annotations
    - Test commands execute without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON file → StatsService | flashcard-stats.json loaded at startup; no network, local file only |
| User input → RoundState | Theme selection drives round creation; no injection surface (desktop app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | flashcard-stats.json | accept | Single-user local app; file is user's own data |
| T-12-02 | Information Disclosure | N/A | accept | No PII, no network, local-only data |
| T-12-03 | Elevation of Privilege | N/A | accept | Single-user desktop; no auth model |
</threat_model>

<verification>
1. `mvn compile -q` — zero errors
2. `mvn test -Dtest="QuestionScoreSM2Test,StatsServiceSM2Test,RoundStateDueReviewTest" -q` — all 13 tests pass
3. `mvn test -q` — full suite passes (existing tests not broken by SM-2 changes)
4. Manual check: flashcard-stats.json backward compat — old files without SM-2 fields still load without error
</verification>

<success_criteria>
- FixationPhase.java exists with 4 enum constants (APRENDENDO, REVISAO, FIXA, DOMINIO)
- QuestionScore has 5 SM-2 fields (easeFactor default 2.5, interval, repCount, lastReviewTimestamp, nextReviewTimestamp)
- QuestionScore.updateSM2(boolean) correctly implements SM-2 binary variant
- QuestionScore.getFixationPhase() returns correct phase per threshold table
- StatsService.recordRound() calls updateSM2() only (old score field no longer updated per D-02)
- StatsService has 7 new methods: getDueQuestions, getDueCount, getNewQuestions, getFixationPhases, getTimelineData, markThemeAsDone, plus updated recordRound
- RoundState.createDueReviewRound() selects overdue questions, falls back to new/unreviewed
- 13 new unit tests pass across 3 test files
- Full test suite passes (existing tests unaffected)
</success_criteria>

<output>
Create `.planning/phases/12-spaced-repetition-visual-review-dashboard/12-01-SUMMARY.md` when done
</output>
