---
phase: 10-visual-theming
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/main/java/org/IsmaelSS/view/StudyRoundView.java
autonomous: false
requirements:
  - VIZ-02
  - VIZ-03
  - VIZ-04
  - VIZ-05
must_haves:
  truths:
    - "StudyRoundView uses CSS classes for all styling, no setStyle() calls remain"
    - "Correct answer highlights green (#27ae60) via CSS class toggle"
    - "Wrong answer highlights red (#e74c3c) via CSS class toggle"
    - "Option buttons reset to default state between questions via CSS class"
    - "Próxima button uses orange accent (#fe9a00) instead of blue (#2196F3)"
    - "Zoom root inline font-size remains untouched"
  artifacts:
    - path: "src/main/java/org/IsmaelSS/view/StudyRoundView.java"
      provides: "Study round view with CSS-class-based theming and dynamic state toggling"
      contains: "getStyleClass"
  key_links:
    - from: "src/main/java/org/IsmaelSS/view/StudyRoundView.java"
      to: "theme.css"
      via: "CSS class toggling for correct/wrong state"
      pattern: "getStyleClass.*\\.(add|remove|setAll)"
    - from: "src/main/java/org/IsmaelSS/view/StudyRoundView.java"
      to: "ScreenController.java"
      via: "Zoom font-size inheritance (root inline)"
      pattern: "setStyle.*font-size"
---

<objective>
Refactor StudyRoundView to use CSS classes for all styling and convert dynamic correct/wrong state changes from setStyle() to getStyleClass().add/remove() toggling.

Purpose: Complete the visual theming migration for the most complex view. StudyRoundView has 11 setStyle() calls including 3 dynamic state changes (correct/wrong highlighting) that require a different approach — class toggling instead of inline style removal.

Output: StudyRoundView.java has zero setStyle() calls. All styling comes from theme.css classes. Correct/wrong feedback works via class toggling. Human verification confirms visual correctness.
</objective>

<execution_context>
@C:/Users/ismae/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/ismae/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-theming/10-CONTEXT.md
@.planning/phases/10-visual-theming/10-RESEARCH.md
@.planning/phases/10-visual-theming/10-01-SUMMARY.md
@src/main/java/org/IsmaelSS/view/StudyRoundView.java
@src/main/resources/styles/theme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor StudyRoundView static setStyle calls to CSS classes</name>
  <files>src/main/java/org/IsmaelSS/view/StudyRoundView.java</files>
  <behavior>
    - After refactoring, StudyRoundView constructor creates all UI elements with CSS class assignments instead of inline setStyle()
    - progressLabel: remove line 36 `setStyle("")` (no-op, just delete the line)
    - questionLabel: replace line 43 `setStyle("-fx-font-weight: bold;")` with `getStyleClass().add("title")`
    - optionButtons loop (lines 50-57): replace line 53 `setStyle("-fx-background-color: #E0E0E0; ...")` with `getStyleClass().add("option-default")`
    - exitButton: replace line 66 `setStyle("-fx-padding: 8 20 8 20;")` with `getStyleClass().add("button-secondary")`
    - proximaButton: replace line 74 `setStyle("-fx-background-color: #2196F3; ...")` with `getStyleClass().add("button-primary")` — NOTE: this changes the button from blue (#2196F3) to orange (#fe9a00) per D-03
    - completionLabel: replace line 93 `setStyle("-fx-font-weight: bold; -fx-text-alignment: center;")` with `getStyleClass().add("title")`
    - voltarButton: replace line 96 `setStyle("-fx-padding: 8 20 8 20;")` with `getStyleClass().add("button-secondary")`
    - setQuestion method (line 119): replace `optionButtons[i].setStyle(...)` with `optionButtons[i].getStyleClass().setAll("option-default")` — this resets all option buttons to default state between questions
    - highlightCorrect method (line 128): replace `optionButtons[correctIndex].setStyle(...)` with `optionButtons[correctIndex].getStyleClass().setAll("option-correct")` — setAll removes all existing classes and adds only "option-correct"
    - highlightWrong method (lines 132-133): replace wrong button setStyle with `optionButtons[wrongIndex].getStyleClass().setAll("option-wrong")` and correct button setStyle with `optionButtons[correctIndex].getStyleClass().setAll("option-correct")`
    - All 11 setStyle() calls in StudyRoundView are removed or converted
    - The zoom setStyle() in ScreenController line 55 is NOT touched (it's a different file)
  </behavior>
  <action>
Modify `src/main/java/org/IsmaelSS/view/StudyRoundView.java` to remove all 11 setStyle() calls and replace them with CSS class assignments:

**In the constructor:**

1. DELETE line 36 entirely: `progressLabel.setStyle("");` — this is a no-op empty string, just remove it.

2. REPLACE line 43: `questionLabel.setStyle("-fx-font-weight: bold;");` → `questionLabel.getStyleClass().add("title");`

3. In the option buttons loop (lines 49-58), REPLACE line 53: `btn.setStyle("-fx-background-color: #E0E0E0; -fx-text-fill: black; -fx-cursor: hand; -fx-padding: 8;");` → `btn.getStyleClass().add("option-default");`

4. REPLACE line 66: `exitButton.setStyle("-fx-padding: 8 20 8 20;");` → `exitButton.getStyleClass().add("button-secondary");`

5. REPLACE line 74: `proximaButton.setStyle("-fx-background-color: #2196F3; -fx-text-fill: white; -fx-padding: 8 20 8 20; -fx-cursor: hand;");` → `proximaButton.getStyleClass().add("button-primary");` — Note: this changes the button color from blue to orange (#fe9a00) per the mandatory accent color decision (D-03).

6. REPLACE line 93: `completionLabel.setStyle("-fx-font-weight: bold; -fx-text-alignment: center;");` → `completionLabel.getStyleClass().add("title");`

7. REPLACE line 96: `voltarButton.setStyle("-fx-padding: 8 20 8 20;");` → `voltarButton.getStyleClass().add("button-secondary");`

**In setQuestion() method (line 119):**

8. REPLACE: `optionButtons[i].setStyle("-fx-background-color: #E0E0E0; -fx-text-fill: black; -fx-cursor: hand; -fx-padding: 8;");` → `optionButtons[i].getStyleClass().setAll("option-default");`
   - `setAll` is used instead of `add` because it also removes any previously added "option-correct" or "option-wrong" classes from the prior question.

**In highlightCorrect() method (line 128):**

9. REPLACE: `optionButtons[correctIndex].setStyle("-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-cursor: hand; -fx-padding: 8;");` → `optionButtons[correctIndex].getStyleClass().setAll("option-correct");`
   - `setAll("option-correct")` removes "option-default" and adds "option-correct".

**In highlightWrong() method (lines 131-133):**

10. REPLACE line 132: `optionButtons[wrongIndex].setStyle("-fx-background-color: #F44336; -fx-text-fill: white; -fx-cursor: hand; -fx-padding: 8;");` → `optionButtons[wrongIndex].getStyleClass().setAll("option-wrong");`

11. REPLACE line 133: `optionButtons[correctIndex].setStyle("-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-cursor: hand; -fx-padding: 8;");` → `optionButtons[correctIndex].getStyleClass().setAll("option-correct");`

After ALL replacements, verify that `grep -c "setStyle" StudyRoundView.java` returns 0. The only setStyle() in the entire project should be the zoom line in ScreenController.

Do NOT modify any other files in this task. The CSS classes (option-default, option-correct, option-wrong, button-primary, button-secondary, title) are already defined in theme.css from Plan 01.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; $count = (grep -c "setStyle" src/main/java/org/IsmaelSS/view/StudyRoundView.java); if ($count -eq 0) { echo "PASS: zero setStyle calls in StudyRoundView" } else { echo "FAIL: $count setStyle calls remain"; exit 1 }; $all = (grep -r "setStyle" src/main/java/org/IsmaelSS/view/); if ($all -eq "") { echo "PASS: zero setStyle calls in all views" } else { echo "FAIL: $all"; exit 1 }; mvn compile -q 2>&1 | Select-String -Pattern "ERROR" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    - StudyRoundView.java has zero setStyle() calls (verified by grep)
    - All views combined have zero setStyle() calls (verified by grep across view/)
    - Option buttons use "option-default", "option-correct", "option-wrong" CSS classes
    - Próxima button uses "button-primary" class (orange accent, not blue)
    - Exit/Voltar buttons use "button-secondary" class
    - setQuestion() resets buttons via getStyleClass().setAll("option-default")
    - highlightCorrect() uses getStyleClass().setAll("option-correct")
    - highlightWrong() uses getStyleClass().setAll("option-wrong") and setAll("option-correct")
    - `mvn compile -q` succeeds with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of themed application</name>
  <what-built>
The complete visual theming system: external CSS stylesheet (theme.css) loaded centrally in ScreenController, all three views refactored from inline setStyle() to CSS class assignments, with dynamic correct/wrong state toggling in StudyRoundView.
  </what-built>
  <how-to-verify>
1. Run: `mvn javafx:run` from the project root
2. **Theme Selection Screen:** Verify dark background (#020817 — very dark navy), white title text "Selecione os temas" in bold, orange "Iniciar" button, and "Relatórios" button with border style
3. **Select a theme and start a round:** Verify the "Próxima" button is now ORANGE (#fe9a00) instead of the old blue (#2196F3)
4. **Answer a question correctly:** Click the correct option — it should turn GREEN (#27ae60)
5. **Answer a question wrong:** Click a wrong option — it should turn RED (#e74c3c), and the correct one turns GREEN
6. **Next question:** Click "Próxima" — all option buttons should reset to light gray (#e0e0e0) default state
7. **Round completion:** Complete the round — verify "Round completo!" text is bold and white on dark background
8. **Reports screen:** Click "Relatórios" — verify dark background, white section titles, Accordion with dark titled-pane headers
9. **Zoom test:** Hold Ctrl + scroll mouse wheel — verify text size changes across all screens (zoom feature still works)
10. **Exit and return:** Click "Voltar" on reports, "Sair" on study round — verify all transitions maintain the dark theme
  </how-to-verify>
  <resume-signal>Type "approved" if all screens show the dark theme correctly with orange accents, or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class toggling | Dynamic state changes via getStyleClass().add/remove(); no external input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation | StudyRoundView highlightCorrect/Wrong | accept | Class toggling is internal logic; no user input flows into class names |
| T-10-04 | Info Disclosure | ProximaButton color change | accept | Blue→orange is cosmetic only; no security impact |
| T-10-SC | Tampering | Maven build | mitigate | No external packages installed; pure code refactoring |
</threat_model>

<verification>
1. `grep -c "setStyle" src/main/java/org/IsmaelSS/view/StudyRoundView.java` returns 0
2. `grep -rc "setStyle" src/main/java/org/IsmaelSS/view/` returns 0 for all view files
3. `grep -c "setStyle" src/main/java/org/IsmaelSS/controller/ScreenController.java` returns 1 (zoom only)
4. `mvn compile -q` passes with zero errors
5. Human visual verification passes for all 10 checkpoints listed above
</verification>

<success_criteria>
- StudyRoundView has zero setStyle() calls — all styling via CSS classes
- Correct/wrong highlighting works via getStyleClass().setAll() toggling
- Option buttons reset between questions via setAll("option-default")
- Próxima button displays orange (#fe9a00) instead of old blue
- All 10 visual verification checkpoints pass
- Zoom (Ctrl+scroll) still works correctly
- `mvn compile -q` succeeds
</success_criteria>

<output>
Create `.planning/phases/10-visual-theming/10-02-SUMMARY.md` when done
</output>
