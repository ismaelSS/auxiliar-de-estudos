---
phase: 10-visual-theming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/styles/theme.css
  - src/main/java/org/IsmaelSS/controller/ScreenController.java
  - src/main/java/org/IsmaelSS/view/ThemeSelectionView.java
  - src/main/java/org/IsmaelSS/view/ReportsView.java
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
  - VIZ-04
  - VIZ-05
must_haves:
  truths:
    - "All scenes load theme.css automatically on registration"
    - "Dark background (#020817) applied to all scene roots"
    - "Orange accent (#fe9a00) used for primary buttons"
    - "White text on dark backgrounds, correct contrast"
    - "No setStyle() calls remain in ThemeSelectionView or ReportsView"
    - "Zoom root inline font-size still works (not overridden by CSS)"
  artifacts:
    - path: "src/main/resources/styles/theme.css"
      provides: "Centralized theme stylesheet with full color palette"
      min_lines: 80
    - path: "src/main/java/org/IsmaelSS/controller/ScreenController.java"
      provides: "CSS loading in registerScreen()"
      contains: "getStylesheets().add"
    - path: "src/main/java/org/IsmaelSS/view/ThemeSelectionView.java"
      provides: "Theme selection with CSS classes instead of inline styles"
      contains: "getStyleClass"
    - path: "src/main/java/org/IsmaelSS/view/ReportsView.java"
      provides: "Reports view with CSS classes instead of inline styles"
      contains: "getStyleClass"
  key_links:
    - from: "src/main/java/org/IsmaelSS/controller/ScreenController.java"
      to: "src/main/resources/styles/theme.css"
      via: "getResource + getStylesheets().add"
      pattern: "getResource.*theme\\.css"
    - from: "src/main/java/org/IsmaelSS/view/ThemeSelectionView.java"
      to: "theme.css"
      via: "CSS class assignments replace inline styles"
      pattern: "getStyleClass.*add"
    - from: "src/main/java/org/IsmaelSS/view/ReportsView.java"
      to: "theme.css"
      via: "CSS class assignments replace inline styles"
      pattern: "getStyleClass.*add"
---

<objective>
Create the external CSS theme stylesheet, wire it into all scenes via ScreenController, and refactor ThemeSelectionView and ReportsView to use CSS classes instead of inline setStyle() calls.

Purpose: Establish the centralized theme as the single source of truth for all visual styling, and convert the two simpler views as a foundation for the more complex StudyRoundView refactoring in Plan 02.

Output: theme.css created, ScreenController loads it, ThemeSelectionView has 0 setStyle() calls, ReportsView has 0 setStyle() calls.
</objective>

<execution_context>
@C:/Users/ismae/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/ismae/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-theming/10-CONTEXT.md
@.planning/phases/10-visual-theming/10-RESEARCH.md
@src/main/java/org/IsmaelSS/controller/ScreenController.java
@src/main/java/org/IsmaelSS/view/ThemeSelectionView.java
@src/main/java/org/IsmaelSS/view/ReportsView.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme.css + wire in ScreenController</name>
  <files>src/main/resources/styles/theme.css, src/main/java/org/IsmaelSS/controller/ScreenController.java</files>
  <action>
Create the directory `src/main/resources/styles/` and the file `src/main/resources/styles/theme.css` with the complete theme stylesheet. The CSS must include ALL of the following sections:

**Global root styling:**
- `.root` selector: `-fx-background-color: #020817;` and `-fx-font-family: "System";`
- Do NOT set `-fx-font-size` on `.root` or any child — zoom (Phase 8) manages font-size via inline style on root, and children inherit from it.

**Layout classes:**
- `.background`: `-fx-background-color: #020817;`
- `.surface`: `-fx-background-color: #1a1a2e;`

**Typography classes:**
- `.title`: `-fx-font-weight: bold; -fx-text-fill: #ffffff; -fx-font-size: 1.4em;`
- `.section-title`: `-fx-font-weight: bold; -fx-text-fill: #ffffff;`
- `.label`: `-fx-text-fill: #e0e0e0;`
- `.error-text`: `-fx-text-fill: #e74c3c;`

**Button classes (per D-03, accent is #fe9a00):**
- `.button-primary`: `-fx-background-color: #fe9a00; -fx-text-fill: #ffffff; -fx-cursor: hand; -fx-padding: 8 20 8 20; -fx-background-radius: 4;`
- `.button-primary:hover`: `-fx-background-color: #ffb340;`
- `.button-primary:pressed`: `-fx-background-color: #cc7c00;`
- `.button-secondary`: `-fx-background-color: transparent; -fx-text-fill: #e0e0e0; -fx-border-color: #333355; -fx-border-radius: 4; -fx-padding: 8 20 8 20; -fx-cursor: hand;`
- `.button-secondary:hover`: `-fx-background-color: #1a1a2e;`

**Option button classes (for StudyRoundView — needed even though Plan 02 updates that view):**
- `.option-default`: `-fx-background-color: #e0e0e0; -fx-text-fill: #000000; -fx-cursor: hand; -fx-padding: 8; -fx-background-radius: 4;`
- `.option-correct`: `-fx-background-color: #27ae60; -fx-text-fill: #ffffff; -fx-cursor: hand; -fx-padding: 8; -fx-background-radius: 4;`
- `.option-wrong`: `-fx-background-color: #e74c3c; -fx-text-fill: #ffffff; -fx-cursor: hand; -fx-padding: 8; -fx-background-radius: 4;`

**Form controls:**
- `.check-box`: `-fx-text-fill: #e0e0e0;`
- `.spinner` and `.spinner .text-field`: dark background styling

**Reports/Accordion styling (per RESEARCH Pitfall 5):**
- `.accordion`: `-fx-background-color: transparent;`
- `.titled-pane > .title`: `-fx-background-color: #1a1a2e; -fx-text-fill: #ffffff;`
- `.titled-pane .content`: `-fx-background-color: #020817; -fx-border-color: #333355;`

**Separator and ScrollPane:**
- `.separator .line`: `-fx-border-color: #333355;`
- `.scroll-pane`: `-fx-background-color: #020817;`
- `.scroll-bar`: `-fx-background-color: #1a1a2e;`
- `.scroll-bar .thumb`: `-fx-background-color: #333355;`

Then modify `ScreenController.java`:
- Add a private method `loadTheme(Scene scene)` that gets the CSS resource via `getClass().getResource("/styles/theme.css").toExternalForm()` and calls `scene.getStylesheets().add(css)`. Add a null-check: if getResource returns null, print a warning to stderr but do not crash.
- In `registerScreen()`, call `loadTheme(scene)` BEFORE `installZoomFilter(scene)` and `applyCurrentFontSize(scene)`. The order matters: CSS loads first, then zoom inline overrides font-size.
- The existing `applyCurrentFontSize()` method with `scene.getRoot().setStyle("-fx-font-size: ...")` MUST remain unchanged — this is the zoom feature per D-04.
- Add import for `java.net.URL` if needed for the null-check.

Do NOT modify any view files in this task.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; mvn compile -q 2>&1 | Select-String -Pattern "ERROR" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    - theme.css exists at src/main/resources/styles/theme.css with all required CSS rules
    - ScreenController.registerScreen() loads theme.css before zoom setup
    - `mvn compile -q` succeeds with zero errors
    - The setStyle() call on line 55 of ScreenController (zoom) is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ThemeSelectionView + ReportsView to CSS classes</name>
  <files>src/main/java/org/IsmaelSS/view/ThemeSelectionView.java, src/main/java/org/IsmaelSS/view/ReportsView.java</files>
  <action>
**ThemeSelectionView.java — per VIZ-05 (consistency):**

1. Line 38 — REMOVE `title.setStyle("-fx-font-weight: bold;");` and REPLACE with `title.getStyleClass().add("title");`

2. Line 53 — REMOVE `feedbackLabel.setStyle("-fx-text-fill: red;");` and REPLACE with `feedbackLabel.getStyleClass().add("error-text");`

That is ALL changes to ThemeSelectionView. The VBox root already gets dark background from the `.root` CSS selector applied globally. Do NOT add any other classes or change any other code in this file.

**ReportsView.java — per VIZ-05 (consistency):**

1. Line 25 — REMOVE `title.setStyle("-fx-font-weight: bold;");` and REPLACE with `title.getStyleClass().add("title");`

2. Line 29 — REMOVE `section1.setStyle("-fx-font-weight: bold;");` and REPLACE with `section1.getStyleClass().add("section-title");`

3. Line 37 — REMOVE `section2.setStyle("-fx-font-weight: bold;");` and REPLACE with `section2.getStyleClass().add("section-title");`

That is ALL changes to ReportsView. The Accordion and TitledPane sub-nodes are styled by CSS selectors (`.titled-pane > .title`, `.titled-pane .content`, etc.) from the theme.css created in Task 1 — no Java code changes needed for those.

After both files are updated, verify with `grep -c "setStyle" <file>` that the count is 0 for both files.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava; $t = (grep -c "setStyle" src/main/java/org/IsmaelSS/view/ThemeSelectionView.java); $r = (grep -c "setStyle" src/main/java/org/IsmaelSS/view/ReportsView.java); if ($t -eq 0 -and $r -eq 0) { echo "PASS: zero setStyle calls" } else { echo "FAIL: ThemeSelectionView=$t ReportsView=$r"; exit 1 }; mvn compile -q 2>&1 | Select-String -Pattern "ERROR" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    - ThemeSelectionView.java has zero setStyle() calls (verified by grep)
    - ReportsView.java has zero setStyle() calls (verified by grep)
    - Both files use getStyleClass().add() with semantic CSS classes
    - `mvn compile -q` succeeds with zero errors
    - ThemeSelectionView title has class "title", feedbackLabel has class "error-text"
    - ReportsView title has class "title", both section labels have class "section-title"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file loading (classpath) | CSS loaded from classpath; malformed CSS could cause visual artifacts but no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | theme.css | accept | CSS is bundled in classpath, not user-editable at runtime; visual-only impact |
| T-10-02 | DoS | ScreenController.loadTheme | mitigate | Null-check on getResource() to prevent NPE; log warning instead of crashing |
| T-10-SC | Tampering | Maven build | mitigate | No external packages installed; pure JavaFX CSS |
</threat_model>

<verification>
1. `mvn compile -q` passes with zero errors
2. `grep -c "setStyle" ThemeSelectionView.java` returns 0
3. `grep -c "setStyle" ReportsView.java` returns 0
4. `grep -c "setStyle" ScreenController.java` returns 1 (the zoom line, which must stay)
5. theme.css exists and contains all required selectors
6. CSS file loads without NPE (null-check in loadTheme)
</verification>

<success_criteria>
- theme.css created with full dark theme palette (#020817 background, #fe9a00 accent)
- ScreenController loads CSS automatically for every registered scene
- ThemeSelectionView: 0 inline styles, uses "title" and "error-text" CSS classes
- ReportsView: 0 inline styles, uses "title" and "section-title" CSS classes
- Zoom feature (root inline font-size) preserved and functional
- Application compiles successfully
</success_criteria>

<output>
Create `.planning/phases/10-visual-theming/10-01-SUMMARY.md` when done
</output>
