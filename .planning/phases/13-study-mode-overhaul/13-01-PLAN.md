---
phase: 13-study-mode-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/IsmaelSS/view/ThemeSelectionView.java
  - src/main/java/org/IsmaelSS/view/ThemeCardNode.java
  - src/main/java/org/IsmaelSS/controller/ThemeSelectionController.java
  - src/main/java/org/IsmaelSS/model/RoundState.java
  - src/test/java/org/IsmaelSS/model/RoundStateCustomStudyTest.java
autonomous: true
requirements: [STUDY-01, STUDY-02, STUDY-03, STUDY-04]

must_haves:
  truths:
    - "Tab label reads 'Estudar' instead of 'Treinar'"
    - "Review button on each theme card reads 'Estudar' instead of 'Revisar'"
    - "Before starting any review round, user sees a dialog to pick question count (5/10/15/20/Todas)"
    - "Default question count is 'Todas' (all questions)"
    - "RoundState.createCustomStudyRound picks questions from multiple themes up to per-theme limit"
    - "RoundState.createDueReviewRound(theme, statsService, maxQuestions) truncates to maxQuestions after shuffle"
    - "ThemeCardNode has a hidden CheckBox that can be toggled visible via setCustomStudyMode()"
    - "isStudySelected() reflects the checkbox state"
    - "All existing tests still pass"
  artifacts:
    - path: "src/main/java/org/IsmaelSS/view/ThemeSelectionView.java"
      provides: "Tab with 'Estudar' label"
      contains: "Estudar"
    - path: "src/main/java/org/IsmaelSS/view/ThemeCardNode.java"
      provides: "Theme card with 'Estudar' button, hidden CheckBox, setCustomStudyMode/isStudySelected methods"
      contains: "setCustomStudyMode"
    - path: "src/main/java/org/IsmaelSS/model/RoundState.java"
      provides: "createCustomStudyRound factory method for multi-theme custom study"
      contains: "createCustomStudyRound"
    - path: "src/test/java/org/IsmaelSS/model/RoundStateCustomStudyTest.java"
      provides: "Unit tests for custom study round creation"
      contains: "@Test"
  key_links:
    - from: "src/main/java/org/IsmaelSS/view/ThemeCardNode.java"
      to: "src/main/resources/styles/theme.css"
      via: "CheckBox styled via .theme-card-checkbox CSS class"
      pattern: "theme-card-checkbox"
    - from: "src/main/java/org/IsmaelSS/model/RoundState.java"
      to: "src/main/java/org/IsmaelSS/model/Theme.java"
      via: "createCustomStudyRound iterates Theme.getQuestions()"
      pattern: "theme.getQuestions"
---

Artifacts this phase produces:
- Tab label "Estudar" in ThemeSelectionView.java — MODIFIED
- Button label "Estudar" in ThemeCardNode.java — MODIFIED
- CheckBox + setCustomStudyMode/isStudySelected on ThemeCardNode.java — MODIFIED
- RoundState.createCustomStudyRound() factory method — MODIFIED
- RoundStateCustomStudyTest.java — NEW test file

---

<objective>
Rename the "Treinar" tab to "Estudar" and the "Revisar" button to "Estudar". Add a question-count selection dialog before starting any review round. Add a hidden CheckBox to ThemeCardNode for future custom study mode. Create the createCustomStudyRound factory method on RoundState for multi-theme rounds. Write unit tests for the custom study factory.

Purpose: This plan establishes the foundational renames and the data-layer factory method that Plan 2's custom study UI depends on. The question count dialog is added to the existing handleReviewTheme flow so it works for both single-theme review and future custom study. ThemeCardNode gains the checkbox infrastructure but keeps it hidden until Plan 2 activates it.

Output: Renamed labels, question count dialog, custom study RoundState factory, ThemeCardNode checkbox support, and passing unit tests.
</objective>

<execution_context>
@C:/Users/ismae/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/ismae/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-study-mode-overhaul/13-CONTEXT.md
@.planning/phases/13-study-mode-overhaul/13-RESEARCH.md
@src/main/java/org/IsmaelSS/view/ThemeSelectionView.java
@src/main/java/org/IsmaelSS/view/ThemeCardNode.java
@src/main/java/org/IsmaelSS/model/RoundState.java
@src/main/java/org/IsmaelSS/controller/ThemeSelectionController.java
@src/test/java/org/IsmaelSS/model/RoundStateDueReviewTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename tab/button + question count dialog + ThemeCardNode checkbox infrastructure</name>
  <files>
    src/main/java/org/IsmaelSS/view/ThemeSelectionView.java,
    src/main/java/org/IsmaelSS/view/ThemeCardNode.java,
    src/main/java/org/IsmaelSS/controller/ThemeSelectionController.java
  </files>
  <read_first>
    src/main/java/org/IsmaelSS/view/ThemeSelectionView.java,
    src/main/java/org/IsmaelSS/view/ThemeCardNode.java,
    src/main/java/org/IsmaelSS/controller/ThemeSelectionController.java,
    src/main/resources/styles/theme.css
  </read_first>
  <action>
**1. ThemeSelectionView.java — Rename tab (per D-01)**

Change line 20 from:
  jogarTab = new Tab("Treinar", new VBox());
to:
  jogarTab = new Tab("Estudar", new VBox());

No other changes to this file.

**2. ThemeCardNode.java — Rename button + add CheckBox infrastructure (per D-02, D-03)**

a) Rename button label: Change line 68 from `revisarBtn = new Button("Revisar")` to `revisarBtn = new Button("Estudar")`.

b) Add CheckBox field: After line 26 (the `feitoBtn` field declaration), add:
  `private final CheckBox studyCheckBox;`

c) Add import: `import javafx.scene.control.CheckBox;`

d) In constructor, after the `feitoBtn` setup (line 74) and before the `buttonRow` creation (line 76), add CheckBox initialization:
  ```java
  studyCheckBox = new CheckBox();
  studyCheckBox.setVisible(false);
  studyCheckBox.setManaged(false);
  studyCheckBox.getStyleClass().add("theme-card-checkbox");
  ```

e) Add CheckBox to badgeRow: Change line 55 from:
  `badgeRow.getChildren().addAll(nameLabel, dominioPrefix, dominioLabel);`
  to:
  `badgeRow.getChildren().addAll(studyCheckBox, nameLabel, dominioPrefix, dominioLabel);`

f) Add 3 new methods after the `refresh()` method (after line 98):
  - `public void setCustomStudyMode(boolean active)` — sets studyCheckBox.setVisible(active) and studyCheckBox.setManaged(active)
  - `public boolean isStudySelected()` — returns studyCheckBox.isSelected()
  - `public void setSelected(boolean selected)` — calls studyCheckBox.setSelected(selected)

**3. ThemeSelectionController.java — Add question count dialog (per D-03)**

a) Add imports:
  - `import javafx.scene.control.Dialog;`
  - `import javafx.scene.control.ComboBox;`
  - `import javafx.scene.control.ButtonType;`
  - `import java.util.Optional;`

b) Add new private method `askQuestionCount()`:
  - Creates a Dialog<Integer> with title "Número de questões" and headerText "Quantas questões por tema?"
  - Adds a ComboBox<String> with items: "5", "10", "15", "20", "Todas" and defaultValue "Todas"
  - ComboBox gets styleClass "combo-box" (existing CSS handles dark theme styling per RESEARCH.md A2)
  - Dialog has OK and CANCEL buttons
  - ResultConverter: if OK, returns "Todas".equals(value) ? Integer.MAX_VALUE : Integer.parseInt(value); if CANCEL returns null
  - Returns result.orElse(0) where 0 means cancelled

c) Modify `handleReviewTheme(Theme theme)` method: Before creating the RoundState, call `int count = askQuestionCount()`. If count == 0 (cancelled), return early. Then create the round using `RoundState.createDueReviewRound(theme, statsService, count)` — the new `maxQuestions` parameter (added in Task 2) truncates the selected questions after shuffle.

NOTE: The question count dialog works for both single-theme review AND custom study rounds (Plan 2). The dialog is shown consistently regardless of mode.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava && mvn compile -q 2>&1 | findstr /i "error" || echo "COMPILE OK"</automated>
  </verify>
  <acceptance_criteria>
    - ThemeSelectionView tab label is "Estudar"
    - ThemeCardNode button label is "Estudar"
    - ThemeCardNode has a CheckBox field (initially hidden)
    - setCustomStudyMode(true) makes CheckBox visible
    - isStudySelected() returns the CheckBox selection state
    - setSelected(true/false) sets the CheckBox state
    - askQuestionCount() shows a Dialog with ComboBox (5/10/15/20/Todas)
    - Returning from dialog with CANCEL does not start a round
    - mvn compile succeeds with zero errors
  </acceptance_criteria>
  <done>
    - Tab "Treinar" renamed to "Estudar" in ThemeSelectionView.java line 20
    - Button "Revisar" renamed to "Estudar" in ThemeCardNode.java line 68
    - studyCheckBox field added to ThemeCardNode (initially hidden, managed=false)
    - setCustomStudyMode(), isStudySelected(), setSelected() methods added to ThemeCardNode
    - askQuestionCount() Dialog added to ThemeSelectionController
    - handleReviewTheme shows question count dialog before starting round
    - mvn compile succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoundState.createCustomStudyRound factory + unit tests</name>
  <files>
    src/main/java/org/IsmaelSS/model/RoundState.java,
    src/test/java/org/IsmaelSS/model/RoundStateCustomStudyTest.java
  </files>
  <read_first>
    src/main/java/org/IsmaelSS/model/RoundState.java,
    src/test/java/org/IsmaelSS/model/RoundStateDueReviewTest.java
  </read_first>
  <behavior>
    - Test 1 (overloadedDueReviewRespectsLimit): createDueReviewRound with 5 questions, limit 3, returns 3 questions
    - Test 2 (overloadedDueReviewLimitExceedsAvailable): createDueReviewRound with 2 questions, limit 10, returns 2 questions
    - Test 3 (mixedThemes): createCustomStudyRound with 2 themes, limit 2 per theme, returns 4 questions total from both themes
    - Test 4 (limitRespected): createCustomStudyRound with 1 theme of 5 questions, limit 3, returns exactly 3 questions
    - Test 5 (limitExceedsAvailable): createCustomStudyRound with 1 theme of 2 questions, limit 10, returns 2 questions (all available)
    - Test 6 (shuffledOptions): each question's shuffledOptions is a permutation of the original options
    - Test 7 (themeNamePreserved): each RoundQuestion's themeName matches the source theme's name
    - Test 8 (emptyThemesList): createCustomStudyRound with empty list returns empty round (0 questions)
  </behavior>
  <action>
**1. Create test file FIRST (RED phase):**

Create `src/test/java/org/IsmaelSS/model/RoundStateCustomStudyTest.java` following the exact pattern from RoundStateDueReviewTest.java:
- Package: org.IsmaelSS.model
- @BeforeEach and @AfterEach delete flashcard-stats.json (same cleanup pattern)
- Helper method `createQ(int id, String text, String... options)` returning Question
- Helper method `collectIds(RoundState state)` iterating and collecting question IDs

Test methods:

a) `overloadedDueReviewRespectsLimit()`: Create a theme with 5 questions, all with repCount=0 (so all are due). Call `RoundState.createDueReviewRound(theme, statsService, 3)`. Assert the round has exactly 3 questions.

b) `overloadedDueReviewLimitExceedsAvailable()`: Create a theme with 2 questions. Call with limit 10. Assert 2 questions returned.

c) `mixedThemes()`: Create 2 themes (t1 with 3 questions, t2 with 3 questions). Call `RoundState.createCustomStudyRound(Arrays.asList(t1, t2), 2, new StatsService())`. Assert the round has exactly 4 questions (2 from t1 + 2 from t2). Collect theme names from the round — assert both "t1" and "t2" are present.

b) `limitRespected()`: Create 1 theme with 5 questions. Call with limit 3. Assert exactly 3 questions returned.

c) `limitExceedsAvailable()`: Create 1 theme with 2 questions. Call with limit 10. Assert exactly 2 questions returned (all available).

d) `shuffledOptionsArePermutation()`: Create 1 theme with 2 questions (options: ["a","b","c","d"]). Call createCustomStudyRound. For each question in the round, collect getCurrentOptions(), sort it, and assert it equals the sorted original options list.

e) `themeNamePreserved()`: Create 2 themes ("alpha", "beta"). Call createCustomStudyRound. Advance through all questions, collecting getCurrentThemeName(). Assert all names are either "alpha" or "beta".

f) `emptyThemesList()`: Call `RoundState.createCustomStudyRound(Collections.emptyList(), 5, new StatsService())`. Assert getSelectedQuestionsCount() == 0 and isComplete() == true.

Note: To access themeName in tests, iterate the round using the existing pattern: while (!state.isComplete()) { state.getCurrentThemeName(); state.advanceToNext(); }

Run the tests — they should FAIL (createCustomStudyRound does not exist yet).

**2. Implement createCustomStudyRound (GREEN phase):**

Add TWO new public static methods to RoundState.java, after the createDueReviewRound method (after line 157):

**Method 1: Overloaded createDueReviewRound with maxQuestions**

```java
/**
 * Creates a due review round with a maximum question limit.
 * Delegates to the existing logic, then truncates after shuffle.
 */
public static RoundState createDueReviewRound(Theme theme, StatsService statsService, int maxQuestions) {
    RoundState full = createDueReviewRound(theme, statsService);
    if (maxQuestions > 0 && maxQuestions < full.getSelectedQuestionsCount()) {
        List<RoundQuestion> truncated = new ArrayList<>(full.getSelectedQuestions().subList(0, maxQuestions));
        return new RoundState(truncated, true);
    }
    return full;
}
```

NOTE: This requires `getSelectedQuestions()` to be accessible. Check if it exists; if not, add a package-private method or refactor the existing createDueReviewRound to accept maxQuestions directly by truncating `selectedQuestions` after the shuffle at line 145.

**Method 2: createCustomStudyRound**

```java
/**
 * Creates a custom study round with questions from multiple selected themes.
 * Each theme contributes up to {@code questionsPerTheme} shuffled questions.
 * All questions from all themes are pooled and shuffled.
 */
public static RoundState createCustomStudyRound(
        List<Theme> selectedThemes, int questionsPerTheme, StatsService statsService) {
    List<RoundQuestion> questions = new ArrayList<>();
    for (Theme theme : selectedThemes) {
        List<Question> themeQuestions = new ArrayList<>(theme.getQuestions());
        Collections.shuffle(themeQuestions);
        int take = Math.min(questionsPerTheme, themeQuestions.size());
        for (int i = 0; i < take; i++) {
            Question q = themeQuestions.get(i);
            List<String> original = q.getOptions();
            String correctText = original.get(q.getCorrect());
            List<String> shuffled = new ArrayList<>(original);
            Collections.shuffle(shuffled);
            int newCorrect = shuffled.indexOf(correctText);
            questions.add(new RoundQuestion(theme.getName(), q, shuffled, newCorrect));
        }
    }
    Collections.shuffle(questions);
    return new RoundState(questions, true);
}
```

This follows the exact same pattern as buildQuestions() (lines 34-52) but accepts a pre-selected list of themes and uses the passed-in questionsPerTheme limit. The StatsService parameter is accepted for API consistency with other factory methods but is not used in this implementation (custom study includes ALL questions, not filtered by due status — per RESEARCH.md Assumption A4).

**3. Run tests — they should PASS (GREEN).**

Run: `mvn test -Dtest=RoundStateCustomStudyTest -q`

Then run full suite: `mvn test -q` to confirm no regressions.
  </action>
  <verify>
    <automated>cd C:\prog\flashCardJava && mvn test -Dtest="RoundStateCustomStudyTest" -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - RoundStateCustomStudyTest.java exists with 8 test methods
    - All 8 tests pass
    - createCustomStudyRound exists as public static method on RoundState
    - createDueReviewRound has an overloaded version accepting maxQuestions parameter
    - Method accepts List<Theme>, int questionsPerTheme, StatsService
    - Each theme contributes up to questionsPerTheme shuffled questions
    - All questions are pooled and globally shuffled
    - Empty themes list returns empty round
    - Full test suite passes (mvn test -q)
  </acceptance_criteria>
  <done>
    - RoundStateCustomStudyTest.java created with 8 tests covering overloaded due review, mixed themes, limit, excess, permutation, theme name, empty list
    - createCustomStudyRound() factory method added to RoundState.java
    - createDueReviewRound(Theme, StatsService, int) overloaded method added
    - All 8 new tests pass
    - Full test suite passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → question count dialog | ComboBox selection drives round size; no file/system access |
| Theme list → RoundState factory | Theme objects drive question selection; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | N/A | accept | Single-user local app; no external input surfaces |
| T-13-02 | Information Disclosure | N/A | accept | No PII, no network, local-only data |
| T-13-03 | Elevation of Privilege | N/A | accept | Single-user desktop; no auth model |
</threat_model>

<verification>
1. `mvn compile -q` — zero errors
2. `mvn test -Dtest="RoundStateCustomStudyTest" -q` — all 8 tests pass
3. `mvn test -q` — full suite passes (existing tests unaffected)
4. Manual check: Tab shows "Estudar", button shows "Estudar", dialog appears before round
</verification>

<success_criteria>
- ThemeSelectionView tab label is "Estudar" (was "Treinar")
- ThemeCardNode button label is "Estudar" (was "Revisar")
- ThemeCardNode has hidden CheckBox with setCustomStudyMode(), isStudySelected(), setSelected()
- askQuestionCount() shows Dialog with ComboBox (5/10/15/20/Todas), default "Todas"
- RoundState.createCustomStudyRound() creates mixed-theme rounds with per-theme question limit
- RoundState.createDueReviewRound(Theme, StatsService, int) creates limited single-theme rounds
- 8 new unit tests pass for custom study and overloaded due review
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/13-study-mode-overhaul/13-01-SUMMARY.md` when done
</output>
